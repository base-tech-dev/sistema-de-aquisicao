{
  "name": "Squad",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Nome do squad"
    },
    "description": {
      "type": "string",
      "description": "Descri\u00e7\u00e3o do squad"
    },
    "team_photo_url": {
      "type": "string",
      "description": "URL da foto do time"
    },
    "team_members": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "role": {
            "type": "string"
          },
          "photo_url": {
            "type": "string"
          },
          "email": {
            "type": "string"
          }
        }
      },
      "description": "Membros do time"
    },
    "client_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "IDs dos clientes do squad"
    },
    "color": {
      "type": "string",
      "enum": [
        "purple",
        "blue",
        "green",
        "orange",
        "red",
        "pink"
      ],
      "default": "purple",
      "description": "Cor do squad"
    }
  },
  "required": [
    "name"
  ]
}
